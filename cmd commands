1. Open Command Prompt (cmd)
Press Windows + R, type cmd, and press Enter.

2. Navigate to Your Project Folder
Replace the path with your actual folder path. For your case:
cd F:\music

3. Initialize Git (if not done already)
git init

4. Check Git Status (optional)
git status

5. Stage All Project Files
git add .

6. Commit Your Changes
git commit -m "Initial commit for Piano Note Detector app"

7. Create a New Repository on GitHub
Go on GitHub website.

Click New repository.

Name it, e.g., piano-note-detector.

Do not initialize with README here.

8. Add Remote Origin (replace URL with your repo’s URL)
git remote add origin https://github.com/yourusername/piano-note-detector.git

9. Rename Your Branch to main (if needed)
git branch -M main

10. Push Local Code to GitHub
git push -u origin main


Summary of commands:
text
cd F:\music
git init
git add .
git commit -m "Initial commit for Piano Note Detector app"
git remote add origin https://github.com/yourusername/piano-note-detector.git
git branch -M main
git push -u origin main
